class Point {
    /**
     * @type {number}
     */
    x;

    /**
     * @type {number}
     */
    y;

    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}

module.exports = Point;
