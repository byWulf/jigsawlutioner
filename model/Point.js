class Point {
    /**
     * @type {number}
     */
    x;

    /**
     * @type {number}
     */
    y;

    /**
     * @type {string}
     */
    color;

    constructor(x, y, color) {
        this.x = x;
        this.y = y;
        this.color = color;
    }
}

module.exports = Point;
